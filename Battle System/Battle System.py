
import pygame
import random
from pygame.locals import (
    K_UP,
    K_DOWN,
    K_LEFT,
    K_RIGHT,
    K_ESCAPE,
    K_z,
)



pygame.init()
#set up variables
battlesystem = True
Position = 0
ButtonNumber = 0
#Game Loop
while battlesystem == True:
    if event.type == pygame.KEYDOWN:
       if Position < ButtonNumber
             if event.key == K.RIGHT:
                Position = Position + 1
        elif Position > -1
            if event.key == K.LEFT:
                Position = Position - 1
        if event.key == K_z:
